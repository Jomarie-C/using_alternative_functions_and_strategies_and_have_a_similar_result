# Pseudocode
    # Ask the user to enter a string
    # Ask the user to enter the prefix to check
    # Create a condition
        # If the input starts with the given prefix
            # Print 'The string starts with the prefix'
        # If the given prefix doesn't match with the given input
            # Print 'The string does not start with the given prefix'