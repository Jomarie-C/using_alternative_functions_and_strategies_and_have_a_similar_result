# Pseudocode
    # Ask the user for an input
    # Ask the user to enter the prefix to be removed
    # Create a condition that will check the given input if it has a prefix and slice it off
        # If the input starts with prefix
            # Remove prefix
            # Display output
        # If the input doesn't have prefix
            # Display output